<?php

namespace JamesRezo\WebHelper\WebServer\ApacheWebServer;

/**
 * <Directory>
 * <DirectoryMatch>
 * <Files>
 * <FilesMatch>
 * <If>
 * <IfDefine>
 * <IfModule>
 * <IfVersion>
 * <Location>
 * <LocationMatch>
 * <Proxy>
 * <ProxyMatch>
 * <VirtualHost>.
 */
class Container
{
    /*private $name;

    private $availability = '';

    private $context;*/ //ServerConfig(s),VirtualHost(v),Directory(d),Htaccess(h)
}
